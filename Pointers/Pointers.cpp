//
// Created by aniket on 4/11/2020.
//
#include "iostream"
#include "conio.h"

//using namespace std;

//int main() {
    //pointer can only point to one data at a specific time
    //declaration
 //   int *ptr, a, **ptr2;
    //pointer second to the another pointer
    // i.e **prt2
//    ptr takes the address of same data type integer variable as shown below
//initialization
/*
    ptr = &a;

    ptr2 = &ptr;

    cout << "The address of int a is :" << ptr << "\n";
    cout << "The address of prt is :" << ptr2;
    cout << "\n\n";
    cout << "After simple operations we have" << "\n";
    ptr += 2;
    cout << "The address of a :" << ptr << "\n";
    ptr2 += 2;
    cout << "The address of ptr :" << ptr2 << "\n";

    return 0;
};*/
